<h2>New Note</h2>
<% form_for(note) do |f| %>
	<%= f.error_messages %>
	
	<%= f.hidden_field :project_id %>
	
	<p>
		<%= f.label :content %><br />
		<%= f.text_area :content %>
	</p>
	
	<p>
		<%= f.submit "Create" %>
	</p>
<% end %>